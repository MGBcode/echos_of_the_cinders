CONSTRUÇÃO DA TELA:
    screenWidth = largura da tela em pixels
    screenHeight = altura da tela em pixels

VARIÁVEIS DE TEMPO E CONTROLES GERAIS:
    deltaTime = armazena o tempo real (em frações de segundo) que se passou entre o frame anterior e o atual. Usado para padronizar velocidades e cronômetros independentemente de quantos FPS o computador está rodando
    inputDir = vetor (X, Y) temporário que capta e armazena a direção que as setas do teclado estão apontando no exato momento

VARIÁVEIS BASE DO JOGADOR (AGORA DENTRO DA STRUCT PLAYER):
    pos = posição atual do jogador na tela (substituiu o antigo playerPos)
    normalSpeed = velocidade base do jogador em pixels por segundo

VARIÁVEIS DO DASH:
    isDashing = verifica se o player está dando dash

    dashSpeedMultiplier = regula a velocidade do dash multiplicando a velocidade base do jogador pelo valor da variável
    dashDuration = duração do dash

    dashCooldown = tempo de espera para iniciar outro dash

    dashTimeCounter = contador utilizado para verificar se o dash não passou do limite de tempo imposto na variável dashDuration (toda vez que a tecla espaço é apertada ela reinicia)

    cooldownTimeCounter = cronômetro que faz a contagem regressiva do tempo de recarga após o fim de um dash. O jogador só pode dar um novo dash quando essa variável chegar a zero ou menos
    dashDirection = direção do dash congelada no momento da ativação, garantindo que o dash siga reto mesmo que o jogador solte as teclas
    
    lasMovingDir = serve para salvar a ultima direção que o player andou,pra caso o player dê dash sem se movimentar,ele ir na direção da ultima andada que ele deu
    diagonalBufferTimer = Timer para validar mudança de direção

VARIÁVEIS DOS ATAQUES LEVE:
    comboStep = é um inteiro acumulativo que funciona como se fosse uma marcha,ela vai de 0 a 3 e representa em ques estado do combo o cavaleiro está
    attackTimer = é o tempo de duração do ataque(tempo que leva do inicio do ataque até o final da ação de atacar)
    
    comboWindowTimer = é o tempo de cooldown após o attackTimer,pois após o personagem atacar ele precisa voltar a uma posição inicial para voltar a receber inputs,tbm serve como janela de tempo em que se o jogador apertar o botão de ataque dnv,o combo continuará
    cooldownattackTimer = cronômetro que faz a contagem regressiva do attackTimer
    
    cooldowncomboWindowTimer = cronômetro que faz a contagem regressiva do comboWindowTimer
    hitboxCenter = Ela define o ponto exato no mapa onde o ataque do player está acontecendo naquele frame.
    hitboxRadius = define o alcance ou a largura do golpe do cavaleiro.
    isHitboxActive = serve para o código saber se o cavaleiro está em estado de ataque ou não,para entao poder criar as hitboxes
    hasHitEnemy = serve para dizer se o cavaleiro está atualmente acertando um hit ou nn em um inimigo

ESTRUTURAS E TIPOS (STRUCTS):
    Player = pacote de dados (struct) que agrupa todas as variáveis do jogador (posição, velocidade, informações do dash). Facilita enviar as informações do jogador de um arquivo para o outro

FUNÇÕES DO JOGADOR (CRIADAS POR NÓS NO PLAYER.C):
    InitPlayer = preenche a variável do tipo Player com os dados iniciais do jogo (coloca no meio da tela, zera os contadores de dash)
    UpdatePlayer = engloba toda a matemática da movimentação, leitura do teclado, cálculo de deltaTime, ativação do dash e diminuição do tempo de recarga
    DrawPlayer = cuida exclusivamente de checar o status do jogador (se está em dash, em recarga ou normal) e desenha os círculos correspondentes na tela

FUNÇÕES DO RAYLIB E RAYMATH UTILIZADAS:
    InitWindow = abre a janela do jogo e define o tamanho e título
    SetTargetFPS = trava a taxa de atualização do jogo em um limite (ex: 60 frames por segundo)

    WindowShouldClose = retorna verdadeiro se o jogador clicou no X de fechar a janela ou apertou ESC
    GetFrameTime = pega o tempo do frame para alimentar a nossa variável deltaTime

    IsKeyDown = verifica se uma tecla está sendo segurada (usado para o movimento constante de andar)
    IsKeyPressed = verifica se a tecla foi apertada uma única vez naquele exato frame (usado para acionar o dash uma vez só)

    Vector2Normalize = função matemática que encurta vetores diagonais. Evita que andar para Cima+Direita faça o personagem correr mais rápido do que andar em linha reta
    BeginDrawing = avisa a placa de vídeo que vamos começar a "pintar" o frame atual

    ClearBackground = pinta a tela inteira de uma cor sólida para apagar o "rastro" do frame passado
    DrawCircleV = desenha um círculo sólido usando um Vector2 como coordenadas centrais

    DrawCircleGradient = desenha um círculo que começa com uma cor no miolo e faz um degradê até as bordas
    DrawText = desenha frases na tela

    TextFormat = mistura texto com variáveis (semelhante ao printf) para conseguir imprimir os números dos cronômetros na tela
    EndDrawing = finaliza a pintura e empurra a imagem pronta para o monitor do jogador
    
    CloseWindow = destroi a janela do jogo e libera a memória quando o programa encerra

MÓDULO DE MAPA (level.h e level.c)

    --> LARGURA_MAPA e ALTURA_MAPA
    São as constantes (#define) que dizem quantos "blocos" o mapa tem de comprimento e altura. Se aumentar esses números, os blocos encolhem e a arena fica maior visualmente.

    --> enum TipeTile
    É a lista de "identidades" que um bloco pode ter (TILE_VAZIO, TILE_PAREDE, TILE_PORTA, etc).

    --> struct Level
    A estrutura principal do mapa. Ela guarda a "grade" (uma matriz/tabela bidimensional que é a planta baixa da sala) e guarda também o tamanho matemático de cada bloco em pixels (tamanho_tile).

    --> level_iniciar()
    É o setup inicial. Ele varre a grade inteira usando "for". Se a posição for na borda da matriz (x=0 ou y=0), ele coloca uma parede. Se for no meio, coloca chão vazio.

    --> level_atualizar_tile()
    Faz a matemática responsiva: pega o tamanho da janela (GetScreenWidth) e divide pela quantidade de colunas. Isso garante que o mapa sempre estique e ocupe 100% da tela perfeitamente, sem sobrar espaço preto.

    --> level_desenhar()
    É o "pintor". Ele roda a 60 frames por segundo dentro da main. Ele varre a matriz inteira e, usando um switch-case, escolhe uma cor dependendo do TipeTile que estiver salvo naquele quadrado, pintando-o na tela com DrawRectangle().

    ============================================================
ARQUIVOS: enemy.h / enemy.c
OBJETIVO: Implementação do sistema de Inteligência Artificial e Combate do Boss.
============================================================
ALTERAÇÕES RECENTES (RESUMO TÉCNICO)
------------------------------------------------------------
player.h = define os campos do jogador, incluindo HP, estamina, morte e estados de ataque.
player.c = implementa a máquina de estados de ataque do jogador, HUD do player e gestão de estamina.
main.c = reposiciona HUDs e aplica dano do boss ao player usando a hitbox ativa.
explicacoes.txt = documenta as mudanças recentes no projeto seguindo o estilo de funções/variáveis.

1. ESTRUTURAS PRINCIPAIS
     - `player.h` : novos campos (`hp`, `stamina`, `alive`, `attackState` etc.)
     - `player.c` : implementação da máquina de estados de ataque, consumo/recovery
                                    de estamina, HUD drawing.
     - `main.c`   : reposicionamento dos HUDs, aplicação de dano do boss ao player
                                    via `Boss_GetAttackCircle()`.



1. ESTRUTURAS PRINCIPAIS
------------------------------------------------------------
- BossState (Enum): Gerencia a Máquina de Estados do inimigo.
    * WANDER: Movimentação aleatória inicial.
    * ALERT: Transição ao detectar o jogador.
    * COMBAT: Perseguição ativa.
    * ATTACK_LIGHT/BRUTAL: Estados de execução de golpes.
    * COOLDOWN: Janela de vulnerabilidade após atacar.

- AttackCircle: Define a área de impacto (hitbox) dos ataques do Boss.
    * center/radius: Localização e tamanho do golpe.
    * damage: Quantidade de vida subtraída do player.
    * active: Flag que determina se o dano deve ser processado.

- Boss (Struct): Armazena todos os dados do inimigo, incluindo HP, 
  posição, velocidades e temporizadores de cada fase de ataque.

2. FUNÇÕES DE LÓGICA (enemy.c)
------------------------------------------------------------
- Boss_Init: 
    Inicializa o Boss com seus atributos de vida e combate. 
    Configura os tempos de 'windup' (preparação) e 'recovery' 
    (recuperação) para equilibrar a dificuldade.

- Boss_Update: 
    Executa a cada frame. Controla a visão do Boss (aggroRadius) 
    e a lógica de movimentação com colisão. Gerencia a transição 
    entre estados e o processamento de dano ao colidir com o Player.

- MoveWithCollision: 
    Sistema de física simples que permite ao Boss deslizar em 
    paredes ao colidir, tratando os eixos X e Y de forma independente.

3. SISTEMA DE COMBATE
------------------------------------------------------------
- Ataque Leve: Golpe rápido com pouco tempo de preparação.
- Ataque Brutal: Inclui uma investida (lunge) em direção à 
  última posição conhecida do jogador, causando dano alto.
- Feedback Visual: O Boss altera sua cor via código conforme o 
  estado (Alerta, Ataque ou Dano) para informar o jogador.

4. VARIÁVEIS GLOBAIS / CONSTANTES LÓGICAS
------------------------------------------------------------
- LARGURA_MAPA / ALTURA_MAPA: Usadas para cálculos de colisão 
  com o cenário (provenientes do level.h).
- deltaTime (dt): Garante que a velocidade do Boss e seus 
  ataques sejam consistentes independente do FPS.
============================================================

============================================================
MÓDULO DE SALA DE TREINO (level.h e level.c - DETALHADO)
============================================================
OBJETIVO: Gerenciar a disposição das salas do jogo, colisões e ambiente responsivo.

ESTRUTURA SALA_TREINO:
    - Dimensões: 25 colunas × 19 linhas de tiles
    - Parede de proteção nas bordas (impede sair da arena)
    - Dummy de treinamento posicionado no centro para praticar combos
    - Porta de saída localizada à direita (transição para SALA_BOSS)

ESTRUTURA SALA_BOSS:
    - Arena de combate contra o chefe
    - Porta bloqueada até o Boss ser derrotado
    - Maior espaço para movimentação em combate

TIPOS DE TILES:
    - TILE_VAZIO: Bloco vazio, totalmente passável
    - TILE_PAREDE: Parede sólida nas bordas do mapa
    - TILE_BLOCO: Obstáculo/bloco colisível (pode ser removido para simplificar layout)
    - TILE_PORTA: Porta/saída de transição entre salas

VARIÁVEIS E FUNÇÕES PRINCIPAIS:

    tamanho_tile = Armazena o tamanho em pixels de cada tile. Calculado dinamicamente
                   para que o mapa sempre preencha 100% da tela sem deformação.
    
    grade[ALTURA_MAPA][LARGURA_MAPA] = Matriz 2D que representa a planta baixa da sala.
                                        Cada posição contém um TipeTile indicando tipo do bloco.

    level_iniciar() = Função de setup. Varre a matriz inteira e preenche com valores iniciais
                      (bordas = paredes, miolo = vazio). Define a primeira sala como SALA_TREINO.

    level_atualizar_tile() = Recalcula tamanho_tile baseado na resolução atual da tela.
                             Essencial ao redimensionar a janela para manter responsividade.
                             Fórmula: tamanho_tile = screenWidth / LARGURA_MAPA

    level_desenhar() = Renderiza todos os tiles na tela a cada frame. Percorre a matriz
                       e usa DrawRectangle com cores diferentes para cada tipo de tile.

    level_pode_mover(pos, raio) = Verifica colisão entre uma posição circular (pos + raio)
                                  e os tiles sólidos (PAREDE, BLOCO). Retorna verdadeiro se
                                  o movimento é permitido, falso se há obstrução.

    level_carregar_sala(sala) = Carrega o layout de uma sala específica. Modifica a grade
                                de acordo com a sala solicitada (SALA_TREINO ou SALA_BOSS).
                                Recalcula tamanho_tile ao mudar de sala.

    level_abrir_saida_boss() = Desbloqueando a porta de saída quando o Boss é derrotado.
                               Muda o TILE_PORTA de bloqueado para passável.

DUMMY DE TREINAMENTO:
    - Posição fixa no centro da SALA_TREINO
    - Raio: 50px (25px Player < 50px Dummy < 70px Boss)
    - HP infinito (999999) para treinar ataques ilimitadamente
    - Não se move nem contra-ataca, apenas recebe dano
    - Útil para testar combos, timing e dano de ataques

============================================================
MÓDULO DE MENU (menu.h e menu.c)
============================================================
OBJETIVO: Gerenciar a interface do usuário, navegação entre telas e exibição do ranking.

ESTRUTURA DO MENU:

TELA PRINCIPAL (Menu_DrawPrincipal):
    - Título do jogo centralizado no topo
    - Botão "JOGAR" - Inicia o jogo na SALA_TREINO
    - Botão "MELHORES TEMPOS" - Exibe o ranking dos top 10 jogadores
    - Efeito hover nos botões (mudam de cor ao passar o mouse)

TELA DE RANKING (Menu_DrawRecordes):
    - Exibe os 10 melhores tempos do arquivo scores.txt
    - Mostra: Posição (1-10), Nome do jogador (3 iniciais) e Tempo em segundos
    - Ordenado de forma ascendente (menor tempo = melhor = primeira posição)
    - Botão "VOLTAR" para retornar à tela principal

TELA DE DERROTA (Menu_DrawDerrota):
    - Fundo vermelho indicando morte do jogador
    - Mensagem "VOCÊ PERDEU"
    - Oferece opção de tentar novamente ou voltar ao menu

TELA DE AGRADECIMENTO (Menu_DrawAgradecimento):
    - Fundo dourado indicando vitória
    - Mensagem de parabéns ao derrotar o Boss
    - Pede as 3 iniciais do nome do jogador para salvar no ranking
    - Campo de input para digitar nome e salvar tempo

VARIÁVEIS PRINCIPAIS:

    isHovering = Flag que detecta se o mouse está sobre um botão.
                 Usada para mudar a cor do botão como feedback visual.

    MouseX, MouseY = Coordenadas atuais do mouse obtidas via GetMouseX/GetMouseY.
                     Usadas para detectar clique nos botões.

    selectedInitial = Índice do caractere atualmente selecionado no input do nome.
    playerInitials[3] = Array de caracteres para armazenar as 3 iniciais do nome.

FUNÇÕES PRINCIPAIS:

    Menu_DrawPrincipal() = Renderiza a tela inicial com titulo, botões JOGAR e MELHORES TEMPOS.
                           Detecta cliques e retorna transição de estado para TREINO ou RECORDES.

    Menu_DrawRecordes() = Exibe os 10 melhores tempos do ranking.
                          Carrega dados de scores.txt usando LoadScores().
                          Permite retornar à tela principal.

    Menu_DrawDerrota() = Exibe tela de morte com fundo vermelho.
                         Oferece reiniciar ou voltar ao menu.

    Menu_DrawAgradecimento() = Exibe tela de vitória com fundo dourado.
                               Interface para digitar 3 iniciais do nome.
                               Valida input e prepara para salvar tempo.

INTERAÇÕES DO USUÁRIO:

    Botões: Clique do mouse para ativar ações (JOGAR, RECORDES, VOLTAR, REINICIAR)
    Input de Nome: Teclado para digitar até 3 caracteres alfanuméricos
    Navegação: Transições entre telas baseadas em estados de jogo

============================================================
MÓDULO DE TIMER (timer.h e timer.c)
============================================================
OBJETIVO: Gerenciar cronômetro in-game, persistência de scores e ranking de leaderboard.

COMPONENTES PRINCIPAIS:

CRONÔMETRO IN-GAME (timer.c):
    - Inicia quando o jogador entra na SALA_TREINO
    - Para quando o Boss é derrotado (vitória)
    - Exibido em HUD no canto superior direito durante gameplay
    - Formato: "Tempo: X.XXs" (segundos com 2 casas decimais)

SISTEMA DE PONTUAÇÃO:

    MIN_VALID_TIME = 1.0f
        Constante que rejeita tempos muito curtos (< 1 segundo).
        Previne exploits onde jogador derrotasse o Boss instantaneamente.

    MAX_SCORES = 10
        Número máximo de posições no ranking. Apenas os top 10 tempos são salvos.

    Score (Struct):
        - initialName[3] = Iniciais do nome do jogador
        - time = Tempo em segundos (float) com precisão de centésimos

ARQUIVO DE PERSISTÊNCIA (scores.txt):
    - Localização: raiz do projeto
    - Formato de linha: "XXX Y.YY\n"
        * XXX = 3 caracteres de iniciais (ex: "ABC")
        * Y.YY = Número float com até 2 casas decimais (ex: "123.45")
        * \n = Quebra de linha após cada entrada

    - Exemplo conteúdo:
        ABC 45.32
        XYZ 67.89
        DEF 50.12

FUNÇÕES DE GERENCIAMENTO:

    SaveScore(const char initialName[3], float time)
        = Salva um novo score se:
          1. O tempo é válido (>= MIN_VALID_TIME)
          2. Carrega scores existentes de scores.txt
          3. Insere o novo score na posição correta (ordenação ascendente)
          4. Mantém apenas os top 10 (remove posição 11 se houver)
          5. Reescreve o arquivo scores.txt com array atualizado
        
        Parâmetros:
            - initialName: Ponteiro para array de 3 caracteres com iniciais
            - time: Tempo em segundos (float)

    LoadScores(Score scores[], int *count)
        = Carrega scores existentes de scores.txt:
          1. Abre arquivo em modo leitura
          2. Percorre linhas e extrai iniciais + tempo
          3. Preenche array 'scores' com dados ordenados
          4. Armazena quantidade de scores lidos em 'count'
          5. Fecha arquivo
        
        Parâmetros:
            - scores: Array de Score para armazenar dados lidos
            - count: Ponteiro para inteiro que receberá quantidade de scores

    ResetTimer()
        = Reinicia o cronômetro para zero. Chamado ao entrar em SALA_TREINO.

    UpdateTimer(float deltaTime)
        = Incrementa o cronômetro baseado em deltaTime.
          Chamado a cada frame dentro de UPDATE_TREINO.

    GetElapsedTime()
        = Retorna o tempo decorrido em segundos.
          Usado em HUD e ao derrotar o Boss.

FLUXO DE JOGO:

    1. Jogador clica JOGAR na tela principal
    2. Estado muda para STATE_TREINO
    3. ResetTimer() é chamado
    4. Cronômetro incrementa a cada frame
    5. Jogador treina e derrota o Boss
    6. Estado muda para STATE_AGRADECIMENTO
    7. GetElapsedTime() fornece o tempo final
    8. Jogador digita 3 iniciais
    9. SaveScore() registra o novo tempo no ranking
    10. Tela de RECORDES mostra posição (ou não, se fora do top 10)

VALIDAÇÃO E SEGURANÇA:

    - Tempos < 1.0s são rejeitados automaticamente
    - Array de scores mantém apenas top 10
    - Arquivo scores.txt é lido/escrito apenas quando necessário
    - Proteção contra corrupção de dados (formato fixo XXX Y.YY)

============================================================